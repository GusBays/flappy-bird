import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    engine: {
        position: 'absolute',
        bottom: 0,
        right: 0,
        left: 0,
        top: 0
    },
    score: {
        fontFamily: 'PressStart2P',
        fontSize: 24,
        color: 'white',
        padding: 0,
        margin: 'auto',
        position: 'absolute',
        top: 10,
        left: 0,
        right: 0,
        textAlign: 'center'
    }
})
